0. Use init() to cache template and bookfile
    - Rename struct Arc to Story
    - Rename Arc.Story to Story.Paragraph
    - refactor the view.html file accordingly

1. Refactor the http.HandleFunc to http.Handler
    - Create struct StoryHandler {
        Template *template.Template
        Chapter map[string]Story
    }
    - Create init() function for StoryHandler
        - load template html files
        - load book from json file
    - Implement ServeHTTP(ResponseWriter, *Request) of Handler interface
        - ExecuteTemplate with template file name and URL path
    - Create StoryHandler instance and call its init() func
    - Put StoryHandler instance to http.ListenAndServe() 

------------------------------------------------------------------

2. Add two more functional options
    - template (multiple templates for choose)
        - Add a html tempate file for test
        - Create a handler function for tempalte option
        - Add ...options to a wrapper of ServeHTTP() ????
    - path (URL path, e.g. /story/new-york)

